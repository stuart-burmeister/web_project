import React from "react";

const UserList = props => {
  const { filter } = props;
  return (
    <div style={{height:"100%", width:"100%", borderWidth:1, borderColor:'#979797', backgroundColor:'cyan'}}>
    </div>
  );
};

export default UserList;