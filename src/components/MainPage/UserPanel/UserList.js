import React from "react";

const UserList = props => {
  const { filter } = props;
  return (
    <div style={{height:"100%", width:"100%", borderColor:'#979797', }}>
    </div>
  );
};

export default UserList;